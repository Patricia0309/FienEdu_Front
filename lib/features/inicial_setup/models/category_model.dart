class Category {
  final String title;
  final String icon; // Usaremos un String para el emoji
  final String description;

  const Category({
    required this.title,
    required this.icon,
    required this.description,
  });
}